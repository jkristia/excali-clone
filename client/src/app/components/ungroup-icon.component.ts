import { Component } from '@angular/core';

/** Two separated squares — the ungroup action. */
@Component({
    selector: 'app-ungroup-icon',
    standalone: true,
    templateUrl: './ungroup-icon.component.html',
})
export class UngroupIconComponent {}
