import { Component } from '@angular/core';

/** Three stacked lines — the menu (hamburger) button. */
@Component({
    selector: 'app-burger-icon',
    standalone: true,
    templateUrl: './burger-icon.component.html',
})
export class BurgerIconComponent {}
