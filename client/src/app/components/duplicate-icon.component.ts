import { Component } from '@angular/core';

/** Two overlapping rounded squares — the duplicate action. */
@Component({
    selector: 'app-duplicate-icon',
    standalone: true,
    templateUrl: './duplicate-icon.component.html',
})
export class DuplicateIconComponent {}
