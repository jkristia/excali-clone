

issues found
## grouping
- opacity not applied to shapes
- edit goup, cannot move text shape
- range selection, no select frame shown for group
- aligning groups does not work
- select a group and do shift-2 (zoom to selection) - it zoom 800% and offset is wrong

## properties
- selecting a single shape should reflect to shapes properties
	- rect: missing update to
		- stroke color
		- fill color
		- fill style
		- stroke width
		- stroke style
	- line / arrow - missing all
	- text missing all

## fix flicker while resizing
- canvas flicker while resizing the browser window

## action
- show group / ungroup in action
- ctrl-shift + ] / [  does not bring to back and to front

## text editing
- to many clicks to edit a text, it should only require 2 clicks
	1 click = select, 1 click = edit mode with the cursor set close to wher the mouse clicked
- double click - select all as now
- resize width, text word wrap 